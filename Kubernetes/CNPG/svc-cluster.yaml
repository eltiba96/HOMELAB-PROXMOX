apiVersion: v1
kind: Service
metadata:
  name: pg-cluster-nodeport
  namespace: default
spec:
  type: NodePort
  selector:
    cnpg.io/cluster: pg-cluster
    role: primary
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      nodePort: 30432