# S3 credentials for CNPG backups.
# Fill in real values locally and apply — do NOT commit the filled-in version.
apiVersion: v1
kind: Secret
metadata:
  name: pg-backup-creds
  namespace: default
type: Opaque
stringData:
  ACCESS_KEY_ID: CHANGE-ME
  SECRET_ACCESS_KEY: CHANGE-ME
